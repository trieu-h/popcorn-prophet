export const currency_formatter = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 });
export const number_formatter = new Intl.NumberFormat('en-US', { maximumFractionDigits: 0 });
